query getFaqById($id: String!) {
    getFaqById(id: $id) {
        id
        url
        metaTitle
        metaDescription
        question
        answer
        featuredFrom
        category
        group
        categoryName
        groupName
        associatedFaqs
    }
}