query product($slug:String){
  product(slug:$slug) @context(provider: "vtex.search-graphql"){
    productId
    link
    specificationGroups{
      specifications{
        values
        name
      }
    }
  }
}
