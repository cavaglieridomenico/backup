query getReview($reviewId: String!,$appKey: String!) {
  getReview(reviewId: $reviewId, appKey: $appKey) @context(provider:"hotpointit.custom-bazaarvoice-whirlpool") { 
    isSyndicated
    syndicateName
    logoImage
  }
}