query getproduct($identifier: ProductUniqueIdentifier) {
    product(identifier: $identifier){
        specificationGroups{
            name
            specifications {
                name
                values
            }
        }
    }
}