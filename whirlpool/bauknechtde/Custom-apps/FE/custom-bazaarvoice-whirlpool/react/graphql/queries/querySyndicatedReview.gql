query getReview($reviewId: String!,$appKey: String!) {
  getReview(reviewId: $reviewId, appKey: $appKey) @context(provider:"bauknechtde.custom-bazaarvoice-whirlpool") { 
    isSyndicated
    syndicateName
    logoImage
  }
}